# Legacy / Orphan Pages

These pages are **not registered** in `App.jsx` routes and are preserved for reference only.

| File | Reason |
|------|--------|
| `TransaksiPage.jsx` | Pre-AppShell layout; superseded by RFID modules |
| `RFIDAuditPage.jsx` | Legacy table UI; not linked in sidebar |

Do not extend. Re-export stubs live at `pages/TransaksiPage.jsx` and `pages/RFIDAuditPage.jsx`.
