# Admin API — required for production build (Vite)
VITE_API_BASE_URL=http://localhost:3000

# Dev LAN (optional): copy to frontend/.env.development — same IP as wali-app/.env.development
# VITE_API_BASE_URL=http://10.25.150.36:3000
